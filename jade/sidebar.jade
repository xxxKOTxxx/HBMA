.shadow
  .shadow-content
    .logo
    .shadow-heading(data-language="common-site-heading") <? echo $language['common']['site']['heading']; ?>
    .shadow-subheading(data-language="common-site-subheading") <? echo $language['common']['site']['subheading']; ?>
.mobile
  .close-block
    .close(data-language="common-mobile-close") <? echo $language['common']['mobile']['close']; ?>
  .menu-block
    .menu(data-language="common-mobile-menu") <? echo $language['common']['mobile']['menu']; ?>
    .logo-block
      .logo-wrap
        .menu-icon.logo
      a.page-link(
        href!="<? echo $language['common']['site']['url']; ?>"
        title!="<? echo $language['common']['site']['title']; ?>"
        data-language!="common-site-title"
      )
        .heading(data-language="common-site-heading") <? echo $language['common']['site']['heading']; ?>
        .subheading(data-language="common-site-subheading") <? echo $language['common']['site']['subheading']; ?>

aside
  nav
    ul.top-menu
      :php $i = 0;
      :php foreach($language['menu']['top'] as $menu_item):
      li.menu-item(class!="<? set((($menu_item['url'] == $page) || ($menu_item['url'] == '.' && $page == 'main')), true, 'current'); set($menu_item['icon'], true, ' icon'); ?>")
        :php if($menu_item["icon"]):
        .menu-icon(class!="<? set($menu_item['icon'], true); ?>")
        :php endif
        a.page-link(
          href!="<? echo $menu_item['url']; ?>"
          title!="<? echo $menu_item['title']; ?>"
          data-language!="menu-top-<? echo $i; ?>"
        )
          .heading(
            data-language!="menu-top-<? echo $i; ?>-heading"
          ) <? echo $menu_item['heading']; ?>
          .subheading(
            data-language!="menu-top-<? echo $i; ?>-subheading"
          ) <? echo $menu_item['subheading']; ?>
      :php $i++;
      :php endforeach
    ul.bottom-menu
      :php $i = 0;
      :php foreach($language['menu']['bottom'] as $menu_item):
      li.menu-item(class!="<? set(($menu_item['url'] == $page), true, 'current'); ?>")
        a.page-link(
          href!="<? echo $menu_item['url']; ?>"
          title!="<? echo $menu_item['title']; ?>"
          data-language!="menu-bottom-<? echo $i; ?>"
        )
          .heading(data-language!="menu-bottom-<? echo $i; ?>-heading") <? echo $menu_item['heading']; ?>
      :php $i++;
      :php endforeach
    .language-menu
      ul.languages
        :php $i = 0;
        :php foreach($language['menu']['languages'] as $menu_item):
        li.menu-item
          a.language(
            class!="<? set(($menu_item['id'] == $_SESSION['language']), true, 'current'); ?>"
            href!=".?language=<? echo $menu_item['id']; ?>"
            title!="<? echo $menu_item['title']; ?>"
            data-language!="menu-languages-<? echo $i; ?>"
            data-set-language!="<? echo $menu_item['id']; ?>"
          ) <? echo $menu_item['heading']; ?>
        :php $i++;
        :php endforeach